## Packages
framer-motion | Essential for smooth layout animations of sorting bars
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- The app uses generator functions to step through sorting/searching algorithms
- Algorithm visualization requires precise control over timing (pausing/speed)
- CSS variables are used for semantic colors (compare, swap, sorted)
